/**
 * Zod validator for {{id}}
 *
 * @remarks
 * Generated from Lexicon schema. DO NOT EDIT manually.
 * Regenerate using: pnpm lexicons:generate
 *
 * @packageDocumentation
 * @internal
 */

import { z } from 'zod';
{{#each imports}}
import { {{#each names}}{{schema}}{{#unless @last}}, {{/unless}}{{/each}} } from '{{path}}.js';
{{/each}}

{{#if isRecord}}
/**
 * Zod schema for {{id}} record.
 *
 * @internal
 */
export const {{camelCaseName}}Schema = z.object({
  {{#each properties}}
  {{@key}}: {{{this}}},
  {{/each}}
});

/**
 * Type for {{id}} record.
 *
 * @public
 */
export type {{pascalCaseName}} = z.infer<typeof {{camelCaseName}}Schema>;
{{/if}}

{{#if isObject}}
/**
 * Zod schema for {{id}} object.
 *
 * @internal
 */
export const {{camelCaseName}}Schema = z.object({
  {{#each properties}}
  {{@key}}: {{{this}}},
  {{/each}}
});

/**
 * Type for {{id}} object.
 *
 * @public
 */
export type {{pascalCaseName}} = z.infer<typeof {{camelCaseName}}Schema>;
{{/if}}

{{#if isQuery}}
/**
 * Zod schema for {{id}} query parameters.
 *
 * @internal
 */
export const {{camelCaseName}}ParamsSchema = z.object({
  {{#each params}}
  {{@key}}: {{{this}}},
  {{/each}}
});

/**
 * Zod schema for {{id}} query output.
 *
 * @internal
 */
export const {{camelCaseName}}OutputSchema = z.object({
  {{#each outputProps}}
  {{@key}}: {{{this}}},
  {{/each}}
});

/**
 * Type for {{id}} query parameters.
 *
 * @public
 */
export type {{pascalCaseName}}Params = z.infer<typeof {{camelCaseName}}ParamsSchema>;

/**
 * Type for {{id}} query output.
 *
 * @public
 */
export type {{pascalCaseName}}Output = z.infer<typeof {{camelCaseName}}OutputSchema>;
{{/if}}
